{
  "name": "empires-eternal",
  "version": "1.0.0",
  "private": true,
  "description": "Empires Eternal — a persistent 24/7 Age of Empires style strategy game with an award-winning landing page.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "echo Empires Eternal dependencies installed. Run: npm run dev",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "preview": "npm run build && npm run start",
    "test": "node --import tsx server/sim-test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
